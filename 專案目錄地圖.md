# 專案目錄地圖

| 路徑 | 責任區 |
|---|---|
| `index.html` | MVP 網站結構：首頁、服務、價目、預約與規則 |
| `styles.css` | 響應式視覺、卡片、表單與手機版樣式 |
| `app.js` | 方案資料、即時估價、跑腿計價與預約需求單 |
| `assets/meihau-booking-logo.png` | 公開頁、會員中心、管理後台與瀏覽器頁籤共用品牌 Logo |
| `assets/ui-v2/` | UI v2 正式 Hero、服務、空間與 Logo 圖片資產；WebP 優先 |
| `platform.css` | 公開首頁、會員中心、管理後台共用 UI v2 tokens、元件與響應式樣式 |
| `platform.js` | 登入、會員、地址、報價、訂單、管理後台與 API 互動；UI v2 未改動此檔 |
| `README.md` | 專案入口、目前狀態與本機預覽方式 |
| `_SESSION_CONTEXT.md` | Codex／Claude Code 共用的當前交接摘要 |
| `協作/骨架定案.md` | 使用者已明確鎖定、不可任意更動的決策 |
| `協作/工作日誌.md` | 依時間追加的工作紀錄 |
| `協作/工作交接區.md` | 任務切換與下一步交接 |
| `協作/除錯紀錄.md` | 錯誤、根因證據與已驗證解法 |

## 目前架構

第一階段採 **零建置相依的靜態網站**：

```text
Browser
  ├─ index.html
  ├─ styles.css
  └─ app.js
       ├─ 服務方案
       ├─ 試營運估價
       └─ 本機需求摘要
```

目前沒有後端、資料庫、API、金流或部署設定。後續如加入正式預約，應另外建立資料層與伺服端權限邊界，不把私密金鑰寫進前端。
