# 工作日誌

## 2026-09-24｜網站品牌 Logo 更換

### 已完成

- 採用使用者指定的 `LOGO/ChatGPT Image 2026年9月24日 下午08_44_21.png`。
- 複製為正式網站資產 `assets/meihau-booking-logo.png`，原始圖檔不修改。
- 公開首頁、會員中心與管理後台的舊「莓」字標記，全部更換為新 Logo 的愛心勾選圖形。
- 三頁 favicon 同步改用新 Logo。
- Playwright 實測桌機與 390px 手機版；Logo 清楚、未被裁掉，導覽列高度與登入按鈕正常。
- `LOGO` 資料夾其餘三張候選圖未納入正式網站與 Git 提交。

## 2026-09-24｜會員全名與多組常用地址部署

### 已完成

- 會員資料拆分為「帳號名稱」與「全名」；帳號名稱可沿用 Google 顯示名稱，全名另行填寫。
- 常用地址支援新增多筆，類型包含住家、公司與其他。
- 每筆地址包含地址名稱、收件人、收件人電話、完整地址與收件位置備註。
- 會員中心改為卡片式地址清單與獨立新增表單，並保留取消新增操作。
- Supabase `booking.members` 新增 `full_name`，`booking.addresses` 新增 `address_type`，既有強制 RLS 保持啟用。
- `booking-api` 已支援更新全名與新增多筆地址，並加入必填、類型與長度驗證。
- 正式程式 commit：`c38b158`；GitHub Pages workflow `35949540337` 已成功。
- 線上靜態頁已讀回確認六項欄位存在；未登入直接進會員頁仍會導回登入入口。

### 尚待使用者操作

- 使用者需親自在 Google 帳號選擇視窗完成登入，才能驗證登入後新增、顯示多筆常用地址的完整流程。
- 不代替使用者操作帳號，也不自行建立正式會員或地址資料。

## 2026-09-24｜前端產品架構 v1 驗收、合併與部署

### 已完成

- 驗收 `feature/full-platform-v1-implementation`，原始交接 commit 為 `aeb4402bb1b9575dda2deb06d33b09f89a22a44e`。
- 修正管理後台在 390px 手機視窗產生 954px 水平溢位的問題；修正後實測為 390／390。
- 新增 `favicon.svg`，正式頁面主控台為 0 error／0 warning。
- 功能分支修正 commit：`186fcdc`。
- 以非強制方式合併至 `main`，合併 commit：`60e243dd4fed8d5af090ca90ef92eb2b540cbbe7`。
- GitHub Pages workflow `35892501346` 成功完成 build、deploy 與 report-build-status。
- 正式首頁、會員中心與管理後台均回傳 HTTP 200；線上後台手機版實測無水平溢位。

### 部署範圍

- 本次部署為前端產品架構 v1 與 Demo 資料。
- 尚未串正式 Google Login、正式會員 API、管理員權限、正式後端計價或綠界付款。
- 本次未修改 Supabase 或其他資料庫。

## 2026-09-23｜接續「脆文經營規劃」並初始化專案

### 目前狀態

- 已讀取 ChatGPT 對話「脆文經營規劃」。
- 本機 `莓好預約站` 原為空資料夾，且 Git 會向上解析到父層倉庫。
- 本輪只建立獨立專案邊界與協作文件，尚未建立產品功能。

### 已完成事項

- 整理專案獨立性與分階段建置原則。
- 建立專案目錄地圖、骨架定案、工作日誌、工作交接區、除錯紀錄與共用交接摘要。
- 在本目錄建立獨立 `.git`，預設分支為 `main`，Git 根目錄不再解析至父層。
- 建立 `.gitignore`，排除私密環境檔、相依套件、建置輸出與暫存紀錄。
- 建立 GitHub Repository `jikong0709/meihau-booking`，設定 `origin` 並推送 `main`。

### 未完成／待確認

- 「莓好預約站」對外品牌是否取代「光言工作室／未熟莓創研室」，或只是預約系統名稱。
- 正式服務清單、價格、營業時間、服務區域、取消規則與責任條款。
- 技術架構、預約資料模型、付款、Google Calendar、後台及部署平台。

### 下次優先動作

1. 確認品牌與第一階段 MVP 邊界。
2. 將試營運價目表轉為待核准規格，不直接視為正式定價。
3. 設計網站資訊架構與預約 Workflow。
4. 技術方案核准後再建立程式碼。

---

## 2026-09-23｜建立第一版 MVP 網站

### 使用者授權

- 使用者要求直接開始架「莓好預約站」。
- 專案已獨立推上 `jikong0709/meihau-booking`，因此開始在該 Repository 建立第一版前端。

### 技術選擇

- 第一階段使用原生 HTML / CSS / JavaScript。
- 不引入框架、套件管理器或建置工具。
- 理由：目前主要目標是先驗證服務分類、價目呈現、估價與預約 Workflow；靜態站可直接本機預覽，也方便之後上 GitHub Pages 或其他靜態平台。

### 已完成

- 建立 `index.html`：品牌首頁、共享辦公、創作空間、跑腿配送、完整試營運價目、預約表單、營運規則。
- 建立 `styles.css`：桌機與手機響應式版型。
- 建立 `app.js`：方案選擇、固定價估算、機車／汽車距離估價、急件倍率、等待費、停靠費、代買服務費與需求單摘要。
- 預約需求單目前只在瀏覽器產生，不會傳到伺服器，不會自動付款或成立正式訂單。
- 同步更新 README、Session Context 與目錄地圖。

### 價格標記

- 頁面明確標示為「MVP 試營運價目」。
- 不把討論中的價目描述成正式上線價格。

### 尚未實作

- 正式預約資料庫與時段鎖定。
- 付款／訂金。
- Google Calendar。
- 自動通知。
- 後台管理。
- 正式部署與網域。

### 下一步

1. 由使用者檢視首頁、服務分類與價目呈現。
2. 核准正式服務與定價規則。
3. 定義正式預約資料模型與時段衝突規則。
4. 再進入資料庫／付款／Calendar 整合。

---

## 2026-09-23｜接入既有 Supabase 並隔離預約資料

### 已完成

- 使用既有 `fb-threads-scheduler` Supabase 專案，不建立新付費專案。
- 新增獨立 `booking` schema、`requests` 與 `submission_limits` 資料表。
- 兩張表均啟用並強制 RLS；`anon`、`authenticated` 沒有資料表權限。
- 建立 service-role-only RPC 與公開 `booking-submit` Edge Function；前端未嵌入任何 Supabase key。
- 前端表單已由「只產生瀏覽器摘要」改為建立 `pending` 需求單。
- 已完成 CORS 預檢、正式 POST 與資料庫 readback 驗收。

### 驗收資料

- 測試需求 ID：`d80bd4d0-3930-4925-b7ad-562e727642ca`。
- 測試資料有明確標記 `Codex驗收測試`、`TEST-NO-CONTACT`，不是正式預約。
- 依刪除紅線保留測試資料，未自行刪除。

### 尚未實作

- 場地／座位時段衝突鎖定。
- 通知與管理後台。
- 付款、訂金與 Google Calendar。

---

## 2026-09-24｜Google 登入、會員預約與後台正式部署

### 已完成

- 在既有 Supabase 的 `booking` schema 新增會員、常用地址、訂單、訂單明細資料表，並完成 RLS 隔離。
- 部署正式 `booking-api` 與公開 runtime config；服務端重新計價，瀏覽器不持有 service role。
- Google Cloud OAuth Web Client 已建立並接入 Supabase Auth；Site URL 與 redirect allow list 指向正式 GitHub Pages。
- 會員中心已接會員資料、地址、試算、訂單建立與我的預約。
- 管理後台已接角色檢查、正式訂單統計、月曆、每日排程與收款篩選。
- `main` 已部署至 commit `273cdd7`，Pages workflow `35946549055` 成功。
- 正式站登入按鈕已實測跳至 Google 選擇帳戶；未登入會員頁保護已通過。

### 待處理

- 使用者需在保留的 Google 分頁親自選擇帳號，之後再驗證首次會員建立與登入後完整操作。
- 第一個需管理權限的帳號不可自動升權；需使用者明確指定後再設定 `admin`。
- 綠界真實付款未啟用；金流設定需另行明確授權。

---

## 2026-09-25｜UI v2 全站改版與正式部署

### 目前狀態

- `feature/ui-v2-full-redesign` 已完成並以 non-fast-forward merge 併入 `main`。
- 功能 commit：`ea9f7bf79e8d1bdf45c9c48a9354e4c57234baf8`；合併 commit：`7438634f0ae59b5353a06b07edf2fec208c33faf`。
- GitHub Pages workflow `36033771729` 已成功；正式站：`https://jikong0709.github.io/meihau-booking/`。

### 已完成

- 依核准參考圖與 WebP 素材重做公開首頁、會員中心與管理後台視覺。
- 公開首頁完成 Hero、四大服務、品牌故事、方案摘要、流程、安心承諾、CTA 與頁尾。
- 會員首頁整理為四個主要入口；既有登入、會員、地址、報價、訂單與 API DOM hooks 保留。
- 管理後台統一 UI tokens，付款狀態與服務狀態改為兩套獨立色彩語意。
- 補齊表單 label、focus-visible、圖片 alt、響應式與 reduced-motion。
- 本機與正式站完成 1440／1024／768／390／320px 檢查；390px 無頁面水平溢位，正式站主控台 0 error。
- 正式站登入入口已到 Google 登入頁；未登入直開會員頁與管理頁皆會導回首頁。

### 改動檔案

- `index.html`、`member.html`、`admin.html`、`platform.css`。
- `assets/ui-v2/` 九個正式圖片資產。
- 未修改 `platform.js`、`config.js`、`app.js`、Supabase、資料庫、Auth、金流、價格計算或後端 API。

### 卡點與下次優先動作

- ⚠️待使用者操作：親自在 Google 頁面選擇帳號，再驗證會員資料、常用地址 CRUD、報價與訂單清單。
- ⚠️待使用者操作：使用已授權管理員帳號驗證後台日曆與收款清單。
- 上述帳號流程未執行前，本輪只能標示「部署完成、登入後驗收待驗證」，不能標最終結案。
